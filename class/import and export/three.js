/* export and import are catagarised as the two parts  
 1.-> default (only one value passed)
 2 ->named  (two or more values are paeesd)

 in named method we call exat name of the variable

 in default method we pass the only one value and it can also acessed as the alias mehod*/

 let a="darshan"
  function impo(){
    console.log("hello good morning")
 }
 module.exports={a,impo}